# 国内镜像加速；偶发返回截断的 packument 元数据，靠下面的重试配置兜底
registry=https://registry.npmmirror.com

# 拉取失败（含响应截断导致的 JSON 解析错误）自动重试，避免镜像抽风时整个启动失败
fetch-retries=5
fetch-retry-mintimeout=2000
fetch-retry-maxtimeout=60000
fetch-timeout=120000
